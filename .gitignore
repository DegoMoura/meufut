# SO
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/
*.swp

# Node (caso adicione build/tooling no futuro)
node_modules/
dist/
.env

# Chaves e credenciais (nunca commitar)
*serviceAccount*.json
*service-account*.json
firebase-adminsdk*.json
*.pem
*.key

# Capacitor: www/ é gerado a partir de index.html/style.css/etc. (rode sync-www.sh) — não
# precisa versionar duas cópias do mesmo app. android/ e ios/ (gerados por "npx cap add")
# ficam versionados normalmente, como o Capacitor recomenda.
package-lock.json
www/
